import 'package:flutter/material.dart';

class Oops extends StatelessWidget {
  const Oops({super.key});

  @override
  Widget build(BuildContext context) {
    /// TODO add click to restart the app
    return const Scaffold(
      body: Center(
        child: Text('error'),
      ),
    );
  }
}
